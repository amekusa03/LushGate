# LushGate 仕様書

山の畑向け、ESP32-C3による自動散水システム。雨の有無だけで散水可否を判断するシンプルな設計。

## 概要

- **目的**: 山の畑（約2坪）の自動散水
- **判断方針**: 精密な土壌水分・水ポテンシャル管理は行わず、「直近の降雨が一定時間に満たなければ散水する」という単純な二値ロジックを採用
- **マイコン**: ESP32-C3
- **ポンプ**: 灯油ストーブ用電動ポンプを流用
- **水源**: 200Lタンク（畑より高所に設置し、自然落差を利用）
- **操作性**: Wi-Fi AP + mDNS (`lushgate.local`) + Web UI による現場スマホ時刻同期・設定変更・履歴確認

## 判断ロジック

1. 判断時刻（例: 毎朝7時、Webで変更可能）を1サイクルの起点とする
2. サイクル中、設定間隔（例: 3分）ごとに雨センサーを読み取り、「濡れ」判定が出た回数×間隔を積算する
3. サイクル終了時点で累積降雨時間が**60分以上（設定可能）**なら、その日は散水をスキップ
4. 60分未満なら散水を実行し、カウンタをリセットして次サイクルへ
5. 散水・スキップ結果はNVS（不揮発性メモリ）の履歴ログに記録される

## 雨センサー（自作・低消費電力）

コンパレータIC搭載の市販モジュールは使わず、電極プレート＋抵抗＋ADC直読の自作回路とする。

### 回路構成
- 電極プレート2枚（ステンレス製）
- 参照抵抗（Rref）: 100kΩ
- GPIO保護抵抗: 1kΩ
- ADCで電圧を読み取り、閾値で濡れ/乾きを判定（Webで閾値調整可能）

### 腐食対策 & 低消費電力
- GPIO0/1を対称接続し、サンプリング毎に極性を反転（電気分解抑制）
- 非サンプリング時（Deep Sleep中）は両ピンを完全Hi-Zとし待機電流ゼロ

## ポンプ駆動

- フォトカプラ絶縁モジュール＋メカリレー（フォトカプラ一次側はESP32から3.3V給電・駆動。リレーコイル12V系/ポンプ3V系とは完全ガルバニック絶縁）
- **3分ON・2分OFF**のデューティで駆動（モーター寿命対策、Webで設定変更可能）
- 総散水時間: 正味10分（設定可能）
- Web画面からの手動テスト散水（30秒自動停止等）に対応

## 現場設定 & ネットワーク (Wi-Fi AP / mDNS / Web)

- **APモード起動**: BOOTボタン（GPIO9）を2秒長押し
- **SoftAP**: SSID `LushGate-XXXX` (オープンまたはパスワード設定)
- **mDNS**: `http://lushgate.local` でブラウザから直結
- **時刻補正**: スマホブラウザから1タップでESP32 RTCへ現在日時を反映
- **NVS保存**: 散水時刻、スリープ間隔、閾値、デューティ設定を不揮発保存
- **履歴参照**: 過去60回分の散水実績（日時、降雨時間、結果、稼働秒数）を表示＆CSV出力
- **自動タイムアウト**: 5分間無操作で安全にDeep Sleepへ自動復帰
